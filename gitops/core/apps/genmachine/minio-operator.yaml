---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: minio-operator
  namespace: argocd
  annotations:
    argocd.argoproj.io/manifest-generate-paths: .;../values
spec:
  goTemplate: true
  generators:
    - git:
        repoURL: 'https://github.com/ixxeL-DevOps/fullstack.git'
        revision: main
        directories:
          - path: 'gitops/manifests/minio-operator/*'
            exclude: false
          - path: 'gitops/manifests/minio-operator/values'
            exclude: true
          - path: 'gitops/manifests/minio-operator/beelink'
            exclude: true
          - path: 'gitops/manifests/minio-operator/k0s'
            exclude: true
  template:
    metadata:
      name: 'minio-operator-{{ .path.basenameNormalized }}'
      annotations:
        argocd.argoproj.io/manifest-generate-paths: .;../values
    spec:
      project: infra-storage
      destination:
        name: '{{ .path.basenameNormalized }}'
        namespace: minio-operator
      sources:
        - path: 'gitops/manifests/minio-operator/{{ .path.basenameNormalized }}'
          repoURL: https://github.com/ixxeL-DevOps/fullstack.git
          targetRevision: main
          helm:
            releaseName: minio-operator
            valueFiles:
              - $values/gitops/manifests/minio-operator/values/common-values.yaml
              - $values/gitops/manifests/minio-operator/{{ .path.basenameNormalized }}/{{ .path.basenameNormalized }}-values.yaml
        - repoURL: https://github.com/ixxeL-DevOps/fullstack.git
          targetRevision: main
          ref: values
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - Validate=true
          - PruneLast=false
          - RespectIgnoreDifferences=true
          - Replace=false
          - ApplyOutOfSyncOnly=true
          - CreateNamespace=true
          - ServerSideApply=true
        retry:
          limit: 4
          backoff:
            duration: 5s
            factor: 2
            maxDuration: 2m
        managedNamespaceMetadata:
          labels:
            pod-security.kubernetes.io/enforce: privileged
